export class Bakery {
  name: string;
  city: string;
  area: string;
  street: string;
  county: string;


  constructor(name: string, city: string, area: string, street: string, county: string) {
    this.name = name;
    this.city = city;
    this.area = area;
    this.street = street;
    this.county = county;
  }
}
