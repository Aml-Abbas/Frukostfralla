export class Bakery {
  name: string;
  area: string;
  street: string;
  house: string;
  city: string;
  county: string;


  constructor(name: string, area: string, street: string, house: string, city: string,
              county: string) {
    this.name = name;
    this.area = area;
    this.street = street;
    this.house = house;
    this.city = city;
    this.county = county;
  }
}
